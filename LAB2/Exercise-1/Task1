1. Initialize a New Git Repository 
cd /path/to/your/directory
mkdir ProjectRepo
cd ProjectRepo
git init
2. Create and Configure the .gitignore File
touch .gitignore
nano .gitignore
*.log
3. Give access
git config --global user.name "Your Name"
git config --global user.email "Your email"
4. Add Files to the Git Repository
git add .
git commit -m "Initial commit with .gitignore to ignore .log files"
5.  Push Your Changes to GitHub
git push -u origin master
6. Add the remote repository:
git remote add origin https://github.com/username/ProjectRepo.git
git remote -v  # To verify the remote URL


